version: "3.9"

services:
  db:
    image: postgres:15
    container_name: receptor_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 04MrOg.26
      POSTGRES_DB: receptor
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: